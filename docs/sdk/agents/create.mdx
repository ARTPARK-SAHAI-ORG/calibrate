---
title: "Create agent"
description: "Create an agent to test inside Calibrate or connect your existing agent to Calibrate"
---

{/* Generated from Fern reference.md — do not edit directly. */}

## Usage

```python
from calibrate import Calibrate

client = Calibrate(
    api_key="your_api_key",
)

client.agents.create(
    name="Support Agent",
    type="agent",
    config={
        "system_prompt": "You are a helpful assistant.",
        "llm": {"model": "google/gemini-2.5-flash"},
        "stt": {"provider": "google"},
        "tts": {"provider": "google"},
        "settings": {"agent_speaks_first": True, "max_assistant_turns": 50}
    },
)
```

## API endpoint

See **POST /agents** in the [API reference](/api-reference/introduction) tab.
